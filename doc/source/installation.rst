============
Installation
============

At the command line::

    $ pip install networking-ovs-dpdk

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv networking-ovs-dpdk
    $ pip install networking-ovs-dpdk
